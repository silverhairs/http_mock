name: http_mock
description: A Dart library that provides a mock for the Client class from the
  Dart http package.
version: 1.0.0

environment:
  sdk: '>=2.17.3 <3.0.0'

dependencies:
  http: ^0.13.4
  mocktail: ^0.3.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
